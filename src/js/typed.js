import Typed from 'typed.js'

var options = {
  strings: ["Software Engineer", 'Team Player', "Problem Solver"],
  typeSpeed: 120,
  backSpeed: 50,
  backDelay: 1500,
  loop: true,
}

var typed = new Typed('#headline', options)
